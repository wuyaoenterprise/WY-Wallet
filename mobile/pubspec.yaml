name: wywallet
description: WY Wallet mobile app with Supabase Google login and Gemini receipt recognition.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.17.2
  fl_chart: ^1.2.0
  image_picker: ^1.2.3
  url_launcher: ^6.3.2
  intl: ^0.20.3

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
